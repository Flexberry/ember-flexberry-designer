import getModelPrimitivesJson from 'dummy/utils/get-model-primitives-json';
import { module, test } from 'qunit';

module('Unit | Utility | get-model-primitives-json', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = getModelPrimitivesJson();
    assert.ok(result);
  });
});

import fdUmlColors from 'dummy/utils/fd-uml-colors';
import { module, test } from 'qunit';

module('Unit | Utility | fd-uml-colors', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = fdUmlColors();
    assert.ok(result);
  });
});

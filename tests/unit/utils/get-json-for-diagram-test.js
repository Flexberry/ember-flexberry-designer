import getJsonForDiagram from 'dummy/utils/get-json-for-diagram';
import { module, test } from 'qunit';

module('Unit | Utility | get json for diagram');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getJsonForDiagram();
  assert.ok(result);
});

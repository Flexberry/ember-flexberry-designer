import { moduleForModel, test } from 'ember-qunit';

moduleForModel('fd-visual-edit-control', 'Unit | Model | fd visual edit control');

test('it exists', function(assert) {
  let model = this.subject();
  assert.ok(!!model);
});
